import first
first.add()
first.sub()


#random()
#this random() method returns a random floating number between 0 and 1 
import random
print(random.random())

#radiant()
#radiant() method returns an intefer number selected element from the specified renge 
import random 
print(random.randint(3,9))

#uniform ()
#uniform() method returns a random floating number between the two specified numbers (both includeed)
import random
print(random.uniform(20,60))

#randrange()
# this randrange() method returns a randomly selected elemetn from the specifies range 
import random
print( random.randrange (3,9))

 #math.cell()
 # the math.ceil method rounds a number up to the nearest intger if necessary and returns the result 
import math 
print(math.ceil(1.1))

#math.floor()
#this rounds of the number down to the nearest integer if neccesary and rerturns the result 
import math 
print(math.floor(5.4))
 
 
#math.fabs() f means float and abs repeeset absolute 
#the math.fabs() method returns the absolute value of a  numbber as a float absolute 
#denotes a non negative number thisremoves the negative sign of the of it has any

import math 
print(math.fabs(-66.55))
print(math.fabs(-568))

#math.sqrt()
# this math.sqrt() method returns the square root of an number 
# note the number must be greated than or equal to 0 

import math 
print(math.sqrt(16))

#divmod()
#divmod() function returns a tupl containing the quotient and the reamainder argumrnt1 (dividend )
# is divided by argument2(divisor )

#there are two types of function  in python 
# 1 built in library function 
# 2 user difind function
# #we use def keyword for creating funtion after that followed by funtion nanmem 
#after creating a funtion we get calling by using the name of the function

#positiomal aggument 